version: 2

models:
    - name: popular_products
      description: "สินค้าขายดี"
      columns:
          - name: product_group
            description: "Primary Key"
            tests:
                - unique
                - not_null